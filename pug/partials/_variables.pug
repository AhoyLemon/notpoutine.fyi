- var testing = false
- var siteTitle = "Not Poutine, FYI."
- var siteURL = "https://notpoutine.fyi"
- var description = "a running collection of menu items which claim to be poutine, but aren't."
- var d = new Date()
- var lastUpdated = d.getFullYear() + '-' + (d.getMonth()<10?'0':'') + d.getMonth() + '-' + (d.getDate()<10?'0':'') + d.getDate() + '@' + (d.getHours()<10?'0':'') + d.getHours() + ':' + (d.getMinutes()<10?'0':'') + d.getMinutes() + ':' + (d.getSeconds()<10?'0':'') + d.getSeconds()